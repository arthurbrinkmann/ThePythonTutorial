import fibo, sys

# Get attributes of the fibo module
fibo_attributes = dir(fibo)
print(f"Fibo module attributes: {fibo_attributes}")

# Get attributes of the sys module
sys_attributes = dir(sys)
print(f"Sys module attributes: {sys_attributes}")
